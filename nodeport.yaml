apiVersion: v1
kind: Service

metadata:
  name: nginx-nodeport

spec:

  type: NodePort

  selector:
    app: webapp

  ports:

  - name: nginx
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
